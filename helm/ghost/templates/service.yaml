apiVersion: v1
kind: Service
metadata:
  name: {{ include "ghost-chart.fullname" . }}
  labels:
    {{- include "ghost-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: 30001
  selector:
    {{- include "ghost-chart.selectorLabels" . | nindent 4 }}
