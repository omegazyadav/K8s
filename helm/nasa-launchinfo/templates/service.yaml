apiVersion: v1
kind: Service
metadata:
  name: {{ include "nasa-launchinfo.fullname" . }}
  labels:
    {{- include "nasa-launchinfo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nasa-launchinfo.selectorLabels" . | nindent 4 }}
