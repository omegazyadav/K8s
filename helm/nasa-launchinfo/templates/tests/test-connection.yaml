apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nasa-launchinfo.fullname" . }}-test-connection"
  labels:
    {{- include "nasa-launchinfo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nasa-launchinfo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
